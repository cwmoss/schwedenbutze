module.exports = {
  sanity: {
    projectId: process.env.GRIDSOME_SANITY_PROJECT_ID || 'emjk7lsc',
    dataset: process.env.GRIDSOME_SANITY_DATASET || 'production',
    token: process.env.GRIDSOME_SANITY_TOKEN
  }
}
